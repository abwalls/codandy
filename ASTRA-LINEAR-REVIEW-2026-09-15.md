# Linear slice and sequence review handoff

Reviewed Claude's observed sequences at 824ae37. Confirmed a defect with a failing regression: collapsing external frames moved the raise marker to an application caller when the final frame was external. The final captured frame is now retained. Trace activation notes clarify that height is a descendant grouping, not a duration scale. Worker checkpoint/structure split passes the backend suite.

Implemented a first Linear vertical slice: local-only settings/status, fixed-host GraphQL with proxy and redirect denial, bounded response/deadline, sanitized errors, viewer check, first 50 teams, reviewed create and safe receipts. Inputs are bounded; exact current credential values and common secret patterns are scrubbed before review/send. Every create requires confirmed=true and the current digest. SQLite commits an uncertain reservation before the request; duplicate key/digest submissions return a confirmed receipt or refuse a second send. Ticket bodies and credentials are not persisted. The ledger is bounded to 10,000 submissions and never silently evicts duplicate-prevention records.

UI entry points: /integrations, Recommended changes, and current whiteboard ticket exports. Stale plans cannot launch ticket creation. Changing title, description or target clears review/confirmation. Native UI controls and existing themes are reused; the home toolbar wraps on small screens.

Validation: full backend run 291 passed with personal report/case/board/ledger hashes unchanged; then all 17 ticket tests passed after adding six HTTP/proxy/redirect tests. All 46 frontend/API contracts, TypeScript, changed-file ESLint, Ruff and production build passed. Headless Edge desktop/mobile mocked creation verified explicit confirmation, review invalidation, receipt link and disabled repeat submission without console errors. No real provider request or ticket creation was performed in QA. Existing dependency deprecation and large-chunk warnings remain.

Review next: ledger failure/recovery and credential-scope handling; provider permissions and live API response on an authorized account. Pending: shared Sentry transport/registry, provider-specific retry-after reporting for reads, pagination beyond 50 teams, projects/labels, inline source-item receipts, source status refresh, and per-task whiteboard tickets. Do not mark full I0/T0/T1 complete or claim live verification. Provider docs: https://linear.app/developers/graphql.
