"""Declared data models and data contracts (structure-0.1), kept separate from atlas 0.2.

Import from the submodules: `models` has no parser dependencies, while `extract` loads the
tree-sitter grammars and belongs in the analysis worker process.
"""
